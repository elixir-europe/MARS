version: '3'

services:
  isa_biosamples:
      build: ./repository-testservers/isajson-biosamples
      ports:
        - "8032:8032"

  isa_sra:
      build: ./repository-testservers/isajson-ena
      ports:
        - "8042:8042"

